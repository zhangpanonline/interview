### 1. Vue的基本原理

#### 1.1 基本原理

当一个Vue实例创建时，Vue会遍历data中的属性，用Object.defineProperty（v3使用proxy）将他们转为 getter/setter，并且在内部追踪相关依赖，在属性被访问和修改时通知变化。每个组件实例都有相应的watcher程序实例，它会在组件渲染的过程中把属性记录为依赖，之后当依赖项的setter被调用时，会通知watcher重新计算，从而使它关联的组件得以更新。

![](https://p9-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/2369052cf3e244d5ac21c9505da97259~tplv-k3u1fbpfcp-zoom-in-crop-mark:1512:0:0:0.awebp?)

#### 1.2 Vue的优点

##### 1. 轻量级框架

只关注视图层，是一个构建数据的视图集合，大小只有几十kb。

##### 2. 简单易学

国人开发，中文文档，不存在语言障碍，易于理解和学习。

##### 3. 双向数据绑定

保留了angular的特点，在数据操作方面更为简单。

##### 4. 组件化

保留了react的优点，实现了html的封装和重用，在构建单页面应用方面有着独特的优势。

##### 5. 视图、数据，结构分离

使数据的更改更为简单，不需要进行逻辑代码的修改，只需要操作数据就能完成相关操作。

##### 6. 虚拟DOM

dom操作是非常耗费性能的，不再使用原生的dom操作节点，极大解放dom操作，但具体操作的还是dom不过是换了另一种方式。

##### 7. 运行速度更快

相比较于 react 而言，同样是操作虚拟dom，就性能而言，vue 存在很大的优势。

### 2. Vue响应式的原理

#### 2.1 什么是数据劫持

利用Object.defineProperty劫持对象的访问器，在属性值发生变化时我们可以获取变化。

#### 2.2 发布者模式  / 订阅者模式

在软件架构中，发布订阅是一种消息范式，消息的发送者（称为发布者）不会将消息直接发送给特定的接收者（称为订阅者）。而是将发布的消息分为不同的类别，无需了解哪些订阅者可能存在。同样的，订阅者可以表达对一个或多个类别的兴趣，只接收感兴趣的消息，无需了解哪些发布者存在。

不同于观察者和被观察者，**发布者和订阅者互相不知道对方的存在，发布者只需要把消息发送到订阅器里面，订阅者只管接受自己需要订阅的内容**。

#### 2.3 响应式原理

Vue响应式的原理就是采用**数据劫持**结合**发布者-订阅者模式**的方式，通过**Object.defineProperty()**来劫持各个属性的setter、getter，在数据变动时发布消息给订阅者，触发相应的监听回调。主要分为以下几个步骤：

**Observe**(被劫持的数据对象) **Compile**(vue的编译器) **Watcher**(订阅者) **Dep**(用于收集Watcher订阅者)

##### 1. 需要给Observe的数据对象及其所有嵌套的子孙对象进行递归遍历，都加上setter和getter这样的属性，给这个对象的某个值赋值，就会触发setter，那么就能监听到了数据变化。

##### 2. Compile解析模板指令，将模板中的变量替换成数据，然后初始化渲染页面视图，并将每个指令对应的节点绑定更新函数，添加监听数据的订阅者，一旦数据有变动，收到通知，更新视图。

##### 3. Watcher订阅者是Observer和Compile之间通信的桥梁，主要做的事情是：

##### 4.

### 3. Object.defineProperty(target, key, options)

#### 3.1 使用方式，有什么缺点

#### 3.2 options 可传什么参数

### 4. MVC、MVP、MVVM

#### 4.1 区别

##### 4.1.1 MVC

##### 4.1.2 MVP

##### 4.1.3 MVVM

#### 4.2 MVVM 的优缺点

##### 优点

##### 缺点

### 5. Vue

#### 5.1 常用指令及作用

#### 5.2 动态绑定Class 与 Style

### 6. 常用修饰符

### 7. Vue内置组件

#### component

#### transition

#### transition-group

#### keep-alive

#### slot

### 8. v-if、v-show、v-html 原理

### 9. v-show 和 v-if 区别

### 10. 为什么避免v-for和v-if在一起使用

### 11. v-for 循环为什么一定要绑定 key

#### 11.1 原因

#### 11.2 为什么不建议用index索引作为key

### 12. v-model 是如何实现的

### 13. v-model 被用在自定义组件上

### 14. computed 和 watch 的区别

### 15. computed 和 Methods 的区别

### 16. 什么是组件

### 17. 什么是插件

#### 17.2 V2 和 V3 怎么注册全局组件

#### 17.3 V2、V3怎么封装自定义插件并使用 Vue.use()

##### Vue2

##### Vue3

### 18. 组件 通信/传值 的方法

#### 什么是组件通信

#### 组件通信解决了什么问题

#### 18.1 props/$emit（父子）

#### 18.2 依赖注入 provide/inject（父子、祖孙）

#### 18.3 ref/$refs（父子、兄弟）

#### 18.4 `$parent/$children` （父子）

#### 18.5 `$attrs/$listeners`（祖孙）

#### 18.6 eventBus 事件总线`$emit/$on`（任意组件通信）

#### 总结

### 19. 子组件可以直接改变父组件的数据吗

### 20. 生命周期

#### 20.1 Vue生命周期

#### 20.2 Vue子组件和父组件执行顺序

##### 加载渲染过程

##### 更新过程

##### 销毁过程

#### 20.3 created 和 mounted 的区别

#### 20.4 一般在哪个生命周期请求一步数据

### 21. 组件缓存 keep-alive

### 22. 过滤器的作用，如何实现一个过滤器

### 23. 说说你对slot插槽的理解，slot使用场景有哪些

#### slot 是什么

#### 默认插槽

#### 具名插槽

#### 作用域插槽

#### 小结

### 24. Vue为什么采用异步渲染呢

### 25. $nextTick 原理及作用

### 26. 描述下V2的自定义指令

#### 26.1 自定义指令基本内容

##### 全局定义

##### 局部定义

##### 钩子函数

##### 钩子函数的参数

#### 26.2 使用场景

#### 26.3 使用案例

### 27. mixin、extends 的覆盖逻辑

#### 27.1 覆盖逻辑

#### 27.2 extend 有什么作用

#### 27.3 什么是 mixin

#### 27.4 mixin 和 mixins 区别

### 28. data 为什么是一个函数而不是对象

### 29. 动态给vue的data添加一个新的属性时会发生什么，如何解决

### 30. Vue data 中某一个属性的值发生改变后，视图会立即同步执行重新渲染吗

### 31. vue 如何监听/监测对象或者数组某个属性的变化

### 32. assets 和 static 的区别

#### 相同点

#### 不同点

#### 建议

### 33. vue的性能优化/项目优化有哪些

#### 1. 编码阶段

#### 2. SEO优化

#### 3. 打包优化

#### 4. 用户体验

### 34. Vue的template模板编译原理

### 35. template 和 jsx 的有什么分别

### 36. 对SSR的理解

#### SSR的优势

#### SSR的缺点

### 37. vue初始化页面闪动问题

### 38. 虚拟DOM

#### 38.1 什么是虚拟DOM

#### 38.2 虚拟DOM的解析过程

#### 38.3 为什么要用虚拟DOM

#### 38.4 虚拟DOM真的比真实DOM性能好吗

### 39. Diff算法

### 40. SPA单页面应用

#### 概念

#### 优点

#### 缺点

#### 解决首次加载慢的问题

### 41. 使用异步组件有什么好处

### 42. 怎样理解 Vue 的单向数据流

### 43. 父组件可以监听到子组件的生命周期吗

### 44. vue 怎么用 vm.$set() 解决对象新增属性不能响应的问题

### 45. scoped作用与原理

#### 作用

#### 原理

### 46. vue中如何扩展一个组件

### 47. vue实例挂载的过程中发生了什么

### 48. 组件中的name属性有什么作用

### 49. 怎么在组件中监听路由参数的变化

### 50. 说说在vue中踩过的坑

### 51. is这个特性是怎么用的

### 52. vue项目的SEO优化

### 53. vue初始化过程中（new Vue(options)）都做了什么

### 54. 什么是函数式组件

### 55. vue2内部如何监听生命周期钩子(hook)





































